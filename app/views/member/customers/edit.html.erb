<div class="member-list">
  <div class="member-list__inner">
    <h1 class="ml-title">Profile <span>Edit</span></h1>

    <section class="form-card">
      <%= form_with model: @created_track do |f| %>
        <div class="auth-form">
          <div class="auth-field">
            <%= f.label :creater_name, "Creator Name" %>
            <%= f.text_field :creater_name, autofocus: true, id: "inputName", placeholder: "Creator Name", class: "auth-input" %>
          </div>
          <div class="auth-field">
            <%= f.label :profile_image, "Profile Image" %>
            <%= attachment_image_tag @member, :profile_image, size: '70x70', fallback: "no_image.jpg", class: "profile-img mb-2" %>
            <%= f.attachment_field :profile_image, class: "auth-input auth-input--file" %>
          </div>
          <div class="auth-field">
            <%= f.label :profile, "Member Information" %>
            <%= f.text_field :profile, class: "auth-input" %>
          </div>
          <%= f.submit 'Save Changes / 変更を保存', class: "neon-btn neon-btn--primary auth-submit" %>
        </div>
      <% end %>
    </section>

    <%= link_to "← Back to Mypage", mypage_member_customers_path, class: "neon-btn neon-btn--ghost hero-logout" %>
  </div>
</div>
