<%= link_to transition_path(notification) do %>
  <div>
    <%= link_to notification.subject.member.first_name, member_path(notification.subject.member) %>
    があなたの
    <%= link_to '投稿', post_like_path(notification.subject.post_like) %>
    にいいねしました
    <%= " (#{time_ago_in_words(notification.created_at)}前)" %>
  </div>
<% end %>