  <div>
    <!--% if notification.subject.follower %>
      ゲストメンバー
    <!--% else %-->
    <%= link_to notification.subject.followed.creater_name, member_top_path(notification.subject.followed) %>
    <!--% end %-->
    があなたをフォローしました
    <%= " (#{time_ago_in_words(notification.created_at)}前)" %>
  </div>
